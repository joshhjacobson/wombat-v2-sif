# Setup region mask as data frame and strip to land regions only

# Construct year-month sequence for study period

# Create inventory index using location and time pairs

# For each id, compute the correlation between SIF and GPP

# Filter for significant correlations (or maybe a heuristic threshold)